# pnpm settings — see https://pnpm.io/settings
#
# Only these packages are allowed to run postinstall scripts.
# strictDepBuilds=true is the v11 default — everything else is blocked.
allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  lmdb: true
  msgpackr-extract: true
